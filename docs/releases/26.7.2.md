> [!IMPORTANT]
> Existing Windows alpha installations cannot update in place to this release. Move aside the existing executable and managed prefix, then install this release into a fresh prefix. Do not reuse the old direct-install metadata.

- Update the bundled conda to 26.7.2 and conda-pypi to 0.12.0, with refreshed package locks for all five platforms.
- Build the native executables with conda-ship 0.9.1 and publish their native update packages under `conda-runtime`.
- Keep conda-self 0.2.1 and conda-runtime-updater 0.1.0 in the managed prefix.
